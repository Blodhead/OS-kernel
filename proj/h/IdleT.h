#ifndef IDLET_H
#define IDLET_H

/*#include "Thread.h"

class IdleThread:Thread{
public:
	IdleThread(StackSize stackSize = defaultStackSize,Time timeSlice = defaultTimeSlice):Thread(stackSize,timeSlice){}
	void run(){while(1){cout<<"IDLE LOOOL"<<endl;}}
	~IdleThread(){waitToComplete();}
};

#endif*/
